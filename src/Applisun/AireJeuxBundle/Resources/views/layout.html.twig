<!DOCTYPE html>
<html>
    <head>
        <meta http-equiv="Content-Type" content="text/html; charset=utf-8" />
        <title>
            {% block title %}
                Aires de jeux française
            {% endblock %}
        </title>
        <meta name="description" content="Les aires de jeux françaises">
        <meta name="viewport" content="width=device-width, initial-scale=1, minimum-scale=1, maximum-scale=1">
        {% block stylesheets %}
            <link rel="stylesheet"  href="http://maxcdn.bootstrapcdn.com/bootstrap/3.3.4/css/bootstrap.min.css" rel="stylesheet">
            <link href="{{ asset('bundles/applisunairejeux/css/main.css') }}" type="text/css" rel="stylesheet" />
        {% endblock%}
    </head>
    <body>
        <nav class="navbar navbar-default navbar-fixed-top orange">
            <div class="container">
                <!-- Brand and toggle get grouped for better mobile display -->
                <div class="navbar-header">
                    <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1">
                        <span class="sr-only">Toggle navigation</span>
                        <span class="icon-bar"></span>
                        <span class="icon-bar"></span>
                        <span class="icon-bar"></span>
                    </button>
                    <a class="navbar-logo" href="{{ path('index') }}">AireJeux.com <i>BETA</i></a>
                </div>

                <!-- Collect the nav links, forms, and other content for toggling -->
                <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
                    <ul class="nav navbar-nav navbar-right">
                        <li><a href="{{path ('aire_new') }}" >Ajouter une aire de jeux</a></li>
                        <li><a href="{{ path('search') }}" >Rechercher</a></li>
                        <li><a href="" >Les contributeurs</a></li>
                        <li><a href="{{path ('fos_user_profile_show') }}">Mon compte</a></li>
                        <li class="login">
                                  {% if is_granted('ROLE_USER') %}
                                    <a class="btn btn-link" href="{{path ('fos_user_security_logout') }}"><span class="glyphicon glyphicon-remove-sign"></span>  Déconnexion</a>
                                  {% else %}
                                    <a class="btn btn-blue" href="{{path ('fos_user_security_login') }}"><span class="glyphicon glyphicon-user"></span> Connexion</a>  
                                  {% endif %}    
                        </li>
                    </ul>
                </div><!-- /.navbar-collapse -->
            </div><!-- /.container-fluid -->
        </nav>        

        <div class="container">
            <div class="span9">
                {% for type, flashMessages in app.session.flashbag.all() %}
                    {% for flashMessage in flashMessages %}
                        <div class="alert alert-{{ type }}">
                            <button type="button" class="close" data-dismiss="alert">×</button>
                            {{ flashMessage | trans({}, 'flash') }}
                        </div>
                    {% endfor %}
                {% endfor %}
            <div class="row contain">
                <section id="contenu" class="col-xs-12 col-md-8">        
                    {% block content %}
                    {% endblock %}
                </section>
            {% block aside %}    
            <aside class="col-md-4 visible-lg visible-md">
                <div id="ad_mobile" class="row nomargin">
                    <div class="col-md-6">
                        <div class="row">
                            <div class="col-md-12">
                                <p>Télécharger bientôt notre application mobile pour smartphone et tablette !!!</p>
                            </div>
                        </div>
                        <div class="row">
                            <div class="col-md-6">
                                <img src="{{ asset('bundles/applisunairejeux/images/android.png') }}" />
                            </div>
                            <div class="col-md-6">
                                <img src="{{ asset('bundles/applisunairejeux/images/apple.png') }}" />
                            </div>
                        </div>
                    </div>
                    <div class="col-md-6">
                        <img class="img-responsive" src="{{ asset('bundles/applisunairejeux/images/appli-airejeux.png') }} " />
                    </div>
                </div>
                <div class="playground-list">
                    <div class="vignette">NOUVEAUTES</div>
                    {{ render_esi(controller('ApplisunAireJeuxBundle:Aire:newest')) }} 
                </div>  
                <div class="playground-list">
                <div class="vignette">TOP 5</div>
                    {{ render_esi(controller('ApplisunAireJeuxBundle:Aire:top')) }}
                </div>
                <div class="playground-list">
                <div class="vignette">+ COMMENTEES</div>
                    {{ render_esi(controller('ApplisunAireJeuxBundle:Aire:moreCommented')) }}
                </div>    
            </aside> 
            {% endblock %}    
            </div>           
    </div>
        </div>  

    {% include 'ApplisunAireJeuxBundle::footer.html.twig' %}

</div>
{% block modal %}
    <!-- Modal -->
    <div class="modal fade" id="myModal" tabindex="-1" role="dialog" 
         aria-labelledby="myModalLabel" aria-hidden="true">

        <div class="modal-dialog">
            <button type="button" class="btn btn-default" 
                    data-dismiss="modal">X
            </button> 
            <div class="modal-content">                                       
            </div>                        
        </div>
    </div>    

    <!-- /.modal -->
{% endblock %}
{% block javascripts %}
    <script src="//ajax.googleapis.com/ajax/libs/jquery/1.8.0/jquery.min.js"></script>
    <script src="{{ asset('bundles/applisunairejeux/js/bootstrap.min.js') }}" type="text/javascript"></script>
    <script src="{{ asset('bundles/applisunairejeux/js/main.js') }}" type="text/javascript"></script>
{% endblock %}
</body>
</html>
