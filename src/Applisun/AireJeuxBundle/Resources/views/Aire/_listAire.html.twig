<ul>
        {% for aire in aires %}
            <li>
               <a href="{{ path('aire_show', {'id': aire.id }) }}">{{ aire.nom }} à {{ aire.ville.nom }}</a> {{ aire.displayedAverage }}/5 
               <i>({{ aire.comments | length }} commentaires)</i>
            </li>
        {% endfor %}    
    </ul>
